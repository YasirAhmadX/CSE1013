// Area of rectangle
#include <stdio.h>

int main() {
    float ar,l,b;
    printf("Enter length and breadth of the rectangle: ");
    scanf("%f %f",&l,&b);
    ar = l*b;
    printf("Area of the rectangle is: %f",ar);

    return 0;
}
